# Raw Amazon datasets

Place your uploaded raw files into matching folders:
- `amazon_books/`
- `amazon_movies/`
- `amazon_beauty/`

Supported input: `.csv` or `.tsv`.
If files are absent, preprocessing can generate toy raw files with `--toy_if_missing`.
